import { GetImageUrlPipe } from './get-image-url.pipe';

describe('GetImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
